# Copy to .dev.vars for local development with `wrangler dev`.
# `.dev.vars` is git-ignored. In production, set these via:
#   wrangler secret put ADMIN_PASSWORD
#   wrangler secret put SESSION_SECRET
ADMIN_PASSWORD="pick-a-local-password"
SESSION_SECRET="any-long-random-string-for-local-dev"
